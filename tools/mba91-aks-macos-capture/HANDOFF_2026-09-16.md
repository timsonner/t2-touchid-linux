# Handoff: Linux → macOS (2026-09-16)

Direction: Omarchy → Sequoia. Branch: `research/mba91-aks-ep7`.
Linux commits since last mac handoff (`9e3ecfd`): `ee3ac7f`, `b3646c3`
(+ this note). Tree clean, pushed.

Do not reset the sensor, do not delete enrollments: SEP now holds
**2 identities** (uid 501). Keep both.

## Linux state (verified warm, read-only)

- `0x42` count **2** (uid 501, prefix), free 1/5, SKS `0x10`,
  `full_snapshot_repeat_equal` true, private inventory complete.
- Biometric port is dynamic (was `49280` — rediscover every boot).
- No `t2_sep` module loaded (correct for match work), no
  `/run/t2-touchid` EP7 session (parked).
- `bridge-xpc-session-selftest.py` (new): **PASS** — 10-step session
  replay, all status 0, zero events, `0x42` byte-identical.

## What Linux proved tonight (session-74)

- **S1** (capture-exact verdict framing + empty 74): prelude all 0,
  74 → **258**. The `48+uid` vs `48-empty` deviation is benign. CLOSED.
- **S2** (full early cluster, no separation, + empty 74): clusters
  all 0, 74 → nil halt-state (open-`4` session hypothesis). Parked,
  do not repeat.
- **S2b** (cluster + `12`-cancel/drain/3 s gap + framing + empty 74):
  sep-cancel 0 with **3 drained events** (the `4` session was live),
  prelude all 0, 74 → clean **258**. `0x42=2` preserved throughout.
- Decoded session map (all status 0, state preserved):
  `48`-empty → 1 B; `39` → 4 B; `46` → **32 B** (needs cap ≥ 32;
  cap 4 → 265, cap 0 → 258); `58` → 33 B (catacomb-hash shape);
  `4`-bare-68 (Fork-A options stand-in) → opens 0.

Reading: framing + ordering are now both capture-exact, yet 74 still
refuses. Remaining doors: (1) the `4`/68 B token *contents* arm 74;
(2) 74 needs `biometrickitd`'s keybag-bound server session (our socket
is anonymous); (3) 74 isn't match-start on this build.

## macOS capture asks (in order)

Shapes/ordering/statuses only — nothing raw leaves `~/Private` + USB.

1. **Between prelude-`4` and verdict-`48`:** exact gap (seconds?),
   any `12`-cancel or teardown call, any service events? Decides
   whether macOS closes the `4` session (S2b assumed yes — confirm).
2. **Doubled verdict-`48` / extra prelude `58`:** how often — only
   first-unlock-after-boot, or every unlock? (Untested S1b candidate.)
3. **Wider window:** opcode 8 or 82 anywhere within ±5 min of an
   unlock (absent in the unlock minute; checking wider).
4. **`4`/68 B call:** exact neighbors (calls immediately before and
   after), confirm ver = 1 / inValue = 0. Contents never leave Private.
5. Keep both enrollments; no Touch ID settings edits (settings UI
   brings its own device-list traffic).

## Reboot notes

- Capture stack on macOS was torn down last trip — reinstall per
  `FORKB_MATCH_TRAFFIC_TRIP_2026-09-13.md` if mining again.
- After mac work: uninstall daemon + profile, warm reboot to Omarchy
  (Restart, NOT Shut Down), no sensor reset — then re-run the
  fingerless self-test + full-inventory before any live window.
