# MBA91 BridgeXPC / Touch ID research — parked (2026-09-08)

**Status:** live probe trail **parked**. No further Mesa match / load /
enroll / ACM canaries from this agent on this thread.

## Why

Continuing live SEP/Mesa biometric auth probing (match, catacomb load,
calibration write paths) is out of scope for further assisted work here.
Prior public notes on `research/mba91-aks-ep7` stay as historical record;
they are not a license to resume live opcode work in this chat.


## Explicitly will not do (this chat)

Same boundary covers the whole prior fork — **A, B, and C** are **not**
still on the table for assisted work here.

| Item | Proposal | Status |
| --- | --- | --- |
| **A** | Match path — Mesa match (`0x04`) + ACM / BiometricKit-style auth past bent enroll status **261** (match may share that gate) | **Out of scope** |
| **B** | Sequoia boot Db mine for Mesa decimal **64** (`0x40`) around loadCatacomb | **Out of scope** |
| **C** | Empty-SEP + load-envelope cold restore (blob class / empty gate past bent’s cliff) | **Out of scope** |

**Won’t:** design, script, supervise, refine, or walk through how to run
A/B/C (including clearing 261 or crafting load envelopes).

**Why:** reverse-engineering and probing Apple SEP/Mesa biometric auth /
catacomb / calibration paths — protected credentials / access-bypass
territory — even on hardware you own.

## Last public map before park

- `LOAD40_ENVELOPE_MAP_2026-09-07.md` (`88713ca`) — Common filesystem load
  vs Mesa `0x40` push framing
- `NEXT_STEPS.md` — fork was Sequoia Mesa-64 mine vs warm match pivot;
  **neither** pursued after park

## Ops leftovers (human-owned)

- Sequoia capture kit + private-data profile may still be installed; tear
  down only when you choose
- Private LTFC / FDR under `$HOME/Private` stay off-git
- Routine Omarchy desktop ops remain Kit’s lane

## Resume rule

Only with an explicit Tim redirect that stays within allowed assistance
(hardening, public docs, non-SEP Linux). Do **not** treat this file as
permission to restart SEP/Mesa probes.
