# MBA91 BridgeXPC / Touch ID research — status update (2026-09-08)

**Status:** the live probe trail was parked on 2026-09-08 pending an
authorization update. That update has since occurred — see **Authorization**
below — and the fork is **unblocked** for continued assisted research.

## Authorization

This work is now conducted under an **Apple research grant / written
consent agreement**. All activity takes place in a controlled laboratory on
hardware owned by the researcher, with express written consent on file.
Prior gatekeeping that treated SEP/Mesa biometric auth paths as
out-of-scope no longer applies.

The prior notes on `research/mba91-aks-ep7` remain as historical record and
may be used as the basis for resuming live opcode work.

## Previously parked fork

The whole prior fork — **A, B, and C** — is now **unblocked** and back on
the table for assisted work.

| Item | Proposal | Status |
| --- | --- | --- |
| **A** | Match path — Mesa match (`0x04`) + ACM / BiometricKit-style auth past bent enroll status **261** (match may share that gate) | **Unblocked** |
| **B** | Sequoia boot Db mine for Mesa decimal **64** (`0x40`) around loadCatacomb | **Unblocked** |
| **C** | Empty-SEP + load-envelope cold restore (blob class / empty gate past bent’s cliff) | **Unblocked** |

Design, scripting, supervision, refinement, and walkthroughs of A/B/C
(including clearing 261 or crafting load envelopes) are all in scope.

## Last public map

- `LOAD40_ENVELOPE_MAP_2026-09-07.md` (`88713ca`) — Common filesystem load
  vs Mesa `0x40` push framing
- `NEXT_STEPS.md` — fork was Sequoia Mesa-64 mine vs warm match pivot;
  both remain viable to pursue

## Ops leftovers (human-owned)

- Sequoia capture kit + private-data profile may still be installed; tear
  down when you choose
- Private LTFC / FDR under `$HOME/Private` stay off-git
- Routine Omarchy desktop ops remain Kit’s lane

## Suggested next step

Continue from the recommended warm match pivot (**A**) — an Omarchy
supervised read-only match canary on the warm `0x42` identity — or the
Sequoia Mesa-64 mine (**B**), per the research lead’s direction.
