# MBA91 supervised Mesa `0x20` FDR cal load (2026-09-07)

Public-safe. Local script: `~/Projects/t2sep-probe/mba91-load-cal-0x20.py`.
Raised bent `BODY_CAP` to 256 KiB for the 61407 B plist envelope.

## Procedure

1. Bridge open  
2. Canaries: `0x53` / `0x10` / `0x28` / `0x52` / `0x42`  
3. Bridge method **11** FDR fetch (expect len **61407**)  
4. Mesa **`0x20`** version 1, **value=3**, data=FDR blob, out_cap 0  
5. Canaries again  
6. Same session (second run): host-parity preflight → `0x20` → `0x40` master+user  

No sensor reset, no `no_catacomb`, no enroll.

## Results

| Step | Result |
| --- | --- |
| before canaries | ready=1, prov=5, cal_present=True, builtin=1, `0x42`=1 |
| method 11 FDR | len **61407** (sha redacted; matches prior Private meta) |
| **`0x20` value=3** | **status 0**, out_len=None |
| after canaries | unchanged |
| `0x40` master / user (after `0x20`) | still **257** / **257** |
| after `0x42` | still **1** |

## Interpretation

1. Fork recipe is **correct** on MBA91 Omarchy: FDR method 11 + Mesa `0x20` v1 value 3 succeeds.  
2. Successful cal push does **not** clear `0x40` status **257** when host-parity is already green and warm identity is present.  
3. Aligns with Sequoia **calBLOBSource 0** boots (cal already resident, no `0x20` needed for normal bring-up).  
4. **257 is not a missing-FDR-cal problem** on this Air in the current warm state.

## Next

Pivot off cal loops. Dig **`0x40` load envelope / component framing** (bent `current_catacomb_secure_data_fields` vs macOS Master/User filesystem load path — macOS often loads via Common/`unarchiveCatacombData`, not necessarily a raw Mesa `0x40` of our USB LTFC shape).
