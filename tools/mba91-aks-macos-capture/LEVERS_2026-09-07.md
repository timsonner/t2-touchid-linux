# Remaining levers — run results (2026-09-07)

Companion to `EP7_FROM_MACOS_NOTES.md`. Host: MacBookAir9,1, macOS 15.7.9
(24G830), research branch `research/mba91-aks-ep7`.

## 1. Proper Boot KC AppleKeyStore extract + disasm — DONE

- Tool: `ipsw kernel extract` v3.1.713 against installed
  `BootKernelExtensions.kc`.
- AKS: UUID `ef0c4f28-1c68-39be-b1f4-4f2e55b11c7c`, SOURCE_VERSION
  **1827.120.2.703.1**, standalone Mach-O in Private only.
- `init_sep_endpoint`: `__ipc_get_capabilities`(v1) → negotiate min(remote,2)
  or default v1 → `set_env(false)` → class-F PRNG entropy.
- `__ipc_get_capabilities`: transport op selector **`0x4d`** (`pushq $0x4d`).
- **No Sequoia-only prerequisite before capabilities.** Aligns with bent’s
  25G83 handoff conclusion; does not explain MBA91 mailbox mute.

## 2. Offline catacomb / CFTL — DONE

See `CATACOMB_ONDISK.md`. NSKeyedArchiver wrapper; `LTFC` v10; user vs master
uid; biolockout `HRLB`; sealed body.

## 3. bridgeOS / SKU delta — PARTIAL

| Fact | Value |
| --- | --- |
| system_profiler iBridge firmware | **23P6068** |
| Marketing iBridge string | 23.16.16068.0.0 |
| bridge-model | J230kAP |
| bent BridgeXPC HELO build (their notes) | **23P6068** |

Same bridgeOS **build string** as bent’s working HELO path. SKU/board still
differs (Air J230kAP vs their MBP proof). Mute EP7 is not “we’re on a wildly
different bridgeOS marketing train” relative to that HELO evidence.

## 4. BridgeXPC Linux biometric path — MAPPED (bent owns depth)

- High-level path: `biometrickitd` → BridgeXPC → `com.apple.eos.BiometricKit`
  / `bkremoted` on bridgeOS — **not** Linux EP7 framing.
- bent status (summary): Multiverse advertises BiometricKit; server-first HELO
  validates as `bkremoted` + BridgeXPC 39 + **23P6068**; method-0 / activation
  still gap; method 3 gated pending exact macOS outbound bytes.
- For MBA91 Omarchy fingerprint UX, this remains the **parallel** track while
  AKS EP7 stays parked mute.

## 5. Raw first-txn capture under macOS — NOT STARTED

Only way to see real EP7 mailbox bytes on this Air under macOS. Options that
need **explicit Tim OK** (SIP / custom kext / hypervisor):

- SIP-off research kext or AppleSEPEndpoint userclient tracing (if exposed)
- Hypervisor / SEP co-processor trace
- PCI/DMA bus capture (heavy)

os_log + biometric Bridge captures will not yield EP7 frames. Left parked
unless scope expands.

## Bottom line

Readable levers that did not need elevated SIP work are exhausted for the
“missing pre-0x4d dance” hypothesis. Next meaningful EP7 move is invasive
first-txn capture or board/SEP bring-up diffs; next meaningful **fingerprint**
move is BridgeXPC activation (bent’s open gap), not more EP7 ABI variants.
