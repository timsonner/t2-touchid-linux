# Alias -501 swap: scratch bag accepted, enroll still 22 (2026-09-25)

Branch: `research/mba91-aks-ep7`. Statuses only. No finger contact.
No form bytes. `keybag.env` was not rewritten.

## Verdict

**Pointing `-501` at the scratch bag did not open enroll.** User 501
still returned dispatch `22`. The alias has been pointed back at the
macOS bag. User 501 still has 2 identities, and its Catacomb query
returns status 0.

The first attempt to bind handle 5 onto `-501` returned SEP `-27`.
That bag UUID was already installed as `-502`, and one UUID cannot
occupy two nonzero special aliases. Unloading only the `-502` entry
returned status 0 and left positive handle 5 in place. Binding handle
5 onto `-501` then returned status 0. Option `0x200` against `-501`
with the saved creation reference returned SEP status 0. Enroll for
uid 501 returned `22`. Cancel was 0. The sensor was not armed.

## Restoration

`-501` was bound back to macOS handle 6 and checked: it names that
bag again. Rebinding the scratch bag onto `-502` returned SEP `-1`.
Alias `-502` is absent. Scratch handle 5 is still loaded. The session
file remains session 1 / handle 6 / special `-501`.
